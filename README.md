# KingMarine's Discord Clone

## Notes for environment variables

### Firebase Service Account Key

Once you've stringified your key through `JSON.stringify()`, make sure you `CTRL/CMD + F` and replace `\\n` with `\n`.
